<!DOCTYPE html>
<html>
    <head>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.5.0/Chart.min.js"></script>
    </head>
    <body>
        <canvas id="householdIncome" style="width: 100%; max-width: 600px"></canvas>
        <script>
            var xValues = ["Bonds", "Walker", "Cob", "Ortiz", "Clemens"];
            var yValues = [1000, 5000, 20000, 15000, 10000];
            var barColors = ["red", "green", "yellow", "blue", "orange"];

            new Chart("householdIncome", {
                type: "bar",
                data: {
                    labels: xValues,
                    datasets: [{
                        backgroundColor: barColors,
                        data: yValues
                    }]
                },
                options: {
                    legend: { display: false },
                    title: {
                        display: true,
                        text: "Annual Income By Household"
                    },
                    scales: {
                        yAxes: [{
                            display: true,
                            ticks: {
                                min: 0
                            }
                        }]
                    }
                }
            });
        </script>
    </body>
</html>
